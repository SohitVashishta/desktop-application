<Window x:Class="SchoolManagementSystem.UI.UI.Views.Library.BookDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Title="Book Details">

    <Grid Margin="25" Width="400">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Book Code -->
        <TextBox Grid.Row="0"
                 x:Name="txtBookCode"
                 Height="32"
                 Padding="8,5"
                 FontSize="10.5"
                 Margin="0,0,0,10"
                 materialDesign:HintAssist.Hint="Book Code"
                 Text="{Binding BookCode, UpdateSourceTrigger=PropertyChanged,ValidatesOnDataErrors=True,
NotifyOnValidationError=True}" />

        <!-- Title -->
        <TextBox Grid.Row="1"
                 x:Name="txtTitle"
                 Height="32"
                 Padding="8,5"
                 FontSize="10.5"
                 Margin="0,0,0,10"
                 materialDesign:HintAssist.Hint="Title"
                 Text="{Binding Title, UpdateSourceTrigger=PropertyChanged,
           ValidatesOnDataErrors=True,
           NotifyOnValidationError=True}" />

        <!-- Author -->
        <TextBox Grid.Row="2"               
                 Height="32"
                 Padding="8,5"
                 FontSize="10.5"
                 Margin="0,0,0,10"
                 materialDesign:HintAssist.Hint="Author"
                 Text="{Binding Author, UpdateSourceTrigger=PropertyChanged,
           ValidatesOnDataErrors=True,
           NotifyOnValidationError=True}" />

        <!-- Category -->
        <TextBox Grid.Row="3"
                 Height="32"
                 Padding="8,5"
                 FontSize="10.5"
                 Margin="0,0,0,10"
                 materialDesign:HintAssist.Hint="Category"
                 Text="{Binding Category}" />

        <!-- Total Copies -->
        <TextBox Grid.Row="4"
                 x:Name="txtTotalCopies"
                 Height="32"
                 Padding="8,5"
                 FontSize="10.5"
                 Margin="0,0,0,20"
                 materialDesign:HintAssist.Hint="Total Copies"
                 Text="{Binding TotalCopies,UpdateSourceTrigger=PropertyChanged,
           ValidatesOnDataErrors=True}" />

        <!-- Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Content="Cancel"
                    Width="80"
                    Margin="0,0,10,0"
                    Command="{Binding CancelCommand}" />

            <Button Content="Save"
                    Width="80"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" />

        </StackPanel>

    </Grid>
</Window>
