<UserControl x:Class="SchoolManagementSystem.UI.UI.Views.StudentManagement.Academic.AcademicYearMasterPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <materialDesign:DialogHost IsOpen="{Binding IsDialogOpen}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ================= TOOLBAR ================= -->
            <Border Grid.Row="0"
                    Background="#F9FAFB"
                    Padding="12"
                    BorderBrush="#E5E7EB"
                    BorderThickness="0,0,0,1">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Academic Years"
                               FontSize="18"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Width="160"
                            Height="40"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#2563EB"
                            Foreground="White"
                            Command="{Binding AddCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CalendarPlus"
                                                     Width="18"
                                                     Height="18"
                                                     Margin="0,0,6,0"/>
                            <TextBlock Text="Add Academic Year"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- ================= GRID ================= -->
            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding AcademicYears}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      RowHeight="48"
                      GridLinesVisibility="None"
                      Style="{StaticResource MaterialDesignDataGrid}">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Academic Year"
                                        Binding="{Binding YearName}"
                                        Width="2*"/>

                    <DataGridTextColumn Header="Start Date"
                                        Binding="{Binding StartDate, StringFormat=d}"
                                        Width="*"/>

                    <DataGridTextColumn Header="End Date"
                                        Binding="{Binding EndDate, StringFormat=d}"
                                        Width="*"/>

                    <DataGridCheckBoxColumn Header="Current"
                                             Binding="{Binding IsCurrent}"
                                             Width="80"/>

                    <DataGridTemplateColumn Width="72">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                        Command="{Binding DataContext.EditCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}">
                                    <materialDesign:PackIcon Kind="PencilOutline"/>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>

            <!-- ================= SNACKBAR ================= -->
            <materialDesign:Snackbar Grid.RowSpan="2"
                                     MessageQueue="{Binding SnackbarMessageQueue}"
                                     VerticalAlignment="Bottom"/>
        </Grid>

        <!-- ================= ADD / EDIT DIALOG ================= -->
        <materialDesign:DialogHost.DialogContent>

            <materialDesign:Card Padding="24" Width="420">

                <StackPanel>

                    <TextBlock Text="{Binding DialogTitle}"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="0,0,0,16"/>

                    <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Academic Year (e.g. 2024–2025)"
                             Text="{Binding EditYear.YearName}"/>

                    <DatePicker Margin="0,12,0,0"
                                SelectedDate="{Binding EditYear.StartDate}"
                                materialDesign:HintAssist.Hint="Start Date"/>

                    <DatePicker Margin="0,12,0,0"
                                SelectedDate="{Binding EditYear.EndDate}"
                                materialDesign:HintAssist.Hint="End Date"/>

                    <CheckBox Content="Set as Current Academic Year"
                              Margin="0,12,0,0"
                              IsChecked="{Binding EditYear.IsCurrent}"/>

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Margin="0,24,0,0">

                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                            Cancel
                        </Button>

                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Margin="12,0,0,0"
                                Command="{Binding SaveCommand}">
                            Save
                        </Button>

                    </StackPanel>

                </StackPanel>
            </materialDesign:Card>

        </materialDesign:DialogHost.DialogContent>

    </materialDesign:DialogHost>
</UserControl>
