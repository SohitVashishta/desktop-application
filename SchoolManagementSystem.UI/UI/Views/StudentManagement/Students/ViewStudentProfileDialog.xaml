<UserControl x:Class="SchoolManagementSystem.UI.UI.Views.StudentManagement.Students.ViewStudentProfileDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <materialDesign:Card Width="480"
                         Padding="20">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <TextBlock Text="Student Profile"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="0,0,0,16"/>

            <!-- CONTENT -->
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Admission No:" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="0" Margin="120,0,0,0" Text="{Binding AdmissionNo}"/>

                <TextBlock Grid.Row="1" Text="Student Name:" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="1" Margin="120,0,0,0" Text="{Binding StudentName}"/>

                <TextBlock Grid.Row="2" Text="Class:" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="2" Margin="120,0,0,0" Text="{Binding ClassName}"/>

                <TextBlock Grid.Row="3" Text="Section:" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="3" Margin="120,0,0,0" Text="{Binding SectionName}"/>

                <TextBlock Grid.Row="4" Text="Gender:" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="4" Margin="120,0,0,0" Text="{Binding Gender}"/>
            </Grid>

            <!-- FOOTER -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">

                <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                    Close
                </Button>

            </StackPanel>
        </Grid>

    </materialDesign:Card>
</UserControl>
