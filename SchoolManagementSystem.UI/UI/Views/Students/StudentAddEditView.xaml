<Window x:Class="SchoolManagementSystem.UI.UI.Views.Students.StudentAddEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Student"
        Height="420"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <TextBlock Text="{Binding Title}"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <!-- FORM -->
        <StackPanel Grid.Row="1">

            <TextBlock Text="First Name"/>
            <TextBox Text="{Binding FirstName}" Margin="0,5,0,10"/>

            <TextBlock Text="Last Name"/>
            <TextBox Text="{Binding LastName}" Margin="0,5,0,10"/>

            <TextBlock Text="Email"/>
            <TextBox Text="{Binding Email}" Margin="0,5,0,10"/>

            <TextBlock Text="Date of Birth"/>
            <DatePicker SelectedDate="{Binding DateOfBirth}" Margin="0,5,0,10"/>

            <TextBlock Text="Enrollment Date"/>
            <DatePicker SelectedDate="{Binding EnrollmentDate}" Margin="0,5,0,10"/>

        </StackPanel>

        <!-- BUTTONS -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">

            <Button Content="Save"
                    Width="90"
                    Command="{Binding SaveCommand}"/>

            <Button Content="Cancel"
                    Width="90"
                    Margin="10,0,0,0"
                    Command="{Binding CancelCommand}"/>

        </StackPanel>
    </Grid>
</Window>
