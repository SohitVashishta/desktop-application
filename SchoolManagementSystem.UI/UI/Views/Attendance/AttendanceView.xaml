<UserControl x:Class="SchoolManagementSystem.UI.UI.Views.Attendance.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ================= HEADER ================= -->
        <StackPanel Margin="0,0,0,15">
            <TextBlock Text="Attendance"
                       FontSize="24"
                       FontWeight="Bold"/>
            <TextBlock Text="Mark and manage daily student attendance"
                       FontSize="12"
                       Foreground="Gray"/>
        </StackPanel>

        <!-- ================= FILTER BAR ================= -->
        <Border Grid.Row="1"
                Background="#F9FAFB"
                Padding="12"
                CornerRadius="6"
                BorderBrush="#E5E7EB"
                BorderThickness="1"
                Margin="0,0,0,15">

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Attendance Date:"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>

                <DatePicker SelectedDate="{Binding SelectedDate}"
                            Width="160"
                            Height="32"/>

                <Button Content="Load"
                        Width="90"
                        Height="32"
                        Margin="15,0,0,0"
                        Command="{Binding LoadCommand}"/>
            </StackPanel>
        </Border>

        <!-- ================= STUDENT LIST ================= -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Students}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  HeadersVisibility="Column"
                  RowHeight="36"
                  GridLinesVisibility="Horizontal">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Roll No"
                                    Binding="{Binding StudentId}"
                                    Width="80"
                                    IsReadOnly="True"/>

                <DataGridTextColumn Header="Student Name"
                                    Binding="{Binding StudentName}"
                                    Width="*"
                                    IsReadOnly="True"/>

                <DataGridCheckBoxColumn Header="Present"
                                        Binding="{Binding IsPresent}"
                                        Width="120"/>

            </DataGrid.Columns>
        </DataGrid>

        <!-- ================= FOOTER ================= -->
        <Border Grid.Row="3"
                Background="#F9FAFB"
                Padding="12"
                CornerRadius="6"
                BorderBrush="#E5E7EB"
                BorderThickness="1"
                Margin="0,15,0,0">

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button Content="Save Attendance"
                        Width="160"
                        Height="36"
                        Background="#2563EB"
                        Foreground="White"
                        BorderThickness="0"
                        Command="{Binding SaveCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
