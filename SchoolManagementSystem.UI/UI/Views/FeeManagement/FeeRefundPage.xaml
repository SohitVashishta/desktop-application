<UserControl x:Class="SchoolManagementSystem.UI.UI.Views.FeeManagement.FeeRefundPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid Background="#F3F4F6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ================= HEADER ================= -->
        <Border Grid.Row="0"
                Background="White"
                Padding="12"
                BorderBrush="#E5E7EB"
                BorderThickness="0,0,0,1">

            <StackPanel>
                <TextBlock Text="Fee Refund"
                           FontSize="20"
                           FontWeight="SemiBold"/>
                <TextBlock Text="Process student fee refunds"
                           FontSize="12"
                           Foreground="#6B7280"/>
            </StackPanel>
        </Border>

        <!-- ================= CONTENT ================= -->
        <Border Grid.Row="1"
                Margin="16"
                Background="White"
                Padding="16"
                CornerRadius="6">

            <StackPanel>

                <TextBlock Text="Refund Details"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Margin="0,0,0,12"/>

                <UniformGrid Columns="2" HorizontalAlignment="Stretch">

                    <!-- STUDENT -->
                    <ComboBox Height="32"
                              Padding="8,5"
                              FontSize="10.5"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              ItemsSource="{Binding Students}"
                              SelectedItem="{Binding SelectedStudent}"
                              DisplayMemberPath="StudentName"
                              materialDesign:HintAssist.Hint="Student *"/>

                    <!-- REFUND AMOUNT -->
                    <TextBox Height="32"
                             Margin="8,0,0,0"
                             Padding="8,5"
                             FontSize="10.5"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Text="{Binding RefundAmount}"
                             materialDesign:HintAssist.Hint="Refund Amount *"/>

                    <!-- REASON -->
                    <TextBox Grid.ColumnSpan="2"
                             Height="72"
                             Margin="0,8,0,0"
                             Padding="8,5"
                             FontSize="10.5"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Text="{Binding RefundReason}"
                             materialDesign:HintAssist.Hint="Refund Reason *"/>

                </UniformGrid>

            </StackPanel>
        </Border>

        <!-- ================= FOOTER ================= -->
        <Border Grid.Row="2"
                Background="White"
                Padding="10"
                BorderBrush="#E5E7EB"
                BorderThickness="1,0,0,0">

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button Height="32"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding CancelCommand}">
                    Cancel
                </Button>

                <Button Height="32"
                        Margin="8,0,0,0"
                        Background="#2563EB"
                        Foreground="White"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding ProcessRefundCommand}">
                    Process Refund
                </Button>

            </StackPanel>
        </Border>

    </Grid>
</UserControl>
