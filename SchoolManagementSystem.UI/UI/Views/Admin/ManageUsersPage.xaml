<UserControl x:Class="SchoolManagementSystem.UI.UI.Views.Admin.ManageUsersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:helpers="clr-namespace:SchoolManagementSystem.UI.UI.Helpers">

    <!-- ================= RESOURCES ================= -->
    <UserControl.Resources>
        <helpers:FirstLetterConverter x:Key="FirstLetterConverter"/>
        <helpers:BoolToStatusBackgroundConverter x:Key="BoolToStatusBackgroundConverter"/>
        <helpers:BoolToStatusTextConverter x:Key="BoolToStatusTextConverter"/>
    </UserControl.Resources>

    <!-- ================= DIALOG HOST ================= -->
    <materialDesign:DialogHost IsOpen="{Binding IsDialogOpen}">

        <!-- ================= MAIN PAGE ================= -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ================= TOOLBAR ================= -->
            <Border Grid.Row="0"
                    Background="#F9FAFB"
                    Padding="12"
                    BorderBrush="#E5E7EB"
                    BorderThickness="0,0,0,1">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- FILTERS -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                        <TextBox Width="160"
                                 Height="40"
                                 Margin="0,0,8,0"
                                 Padding="12,8"
                                 FontSize="12"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="Search"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                        <ComboBox Width="150"
                                  Height="40"
                                  Margin="0,0,8,0"
                                  Padding="12,8"
                                  FontSize="12"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                  ItemsSource="{Binding Roles}"
                                  SelectedItem="{Binding SelectedRole}"
                                  materialDesign:HintAssist.Hint="Role"/>

                        <ComboBox Width="140"
                                  Height="40"
                                  Padding="12,8"
                                  FontSize="12"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                  ItemsSource="{Binding StatusOptions}"
                                  SelectedItem="{Binding SelectedStatus}"
                                  materialDesign:HintAssist.Hint="Status"/>

                        <Border Width="16"/>

                        <Button Width="100"
                                Height="40"
                                FontSize="12"
                                Style="{StaticResource MaterialDesignFlatButton}"
                                Background="#E5E7EB"
                                Command="{Binding ResetFiltersCommand}">
                            Reset
                        </Button>
                    </StackPanel>

                    <!-- ADD USER -->
                    <Button Grid.Column="1"
                            Width="140"
                            Height="40"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#2563EB"
                            Foreground="White"
                            Command="{Binding AddUserCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="AccountPlus" Width="18" Height="18" Margin="0,0,6,0"/>
                            <TextBlock Text="Add User" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- ================= USERS GRID ================= -->
            <Border Grid.Row="1" Background="White" ClipToBounds="True">

                <DataGrid ItemsSource="{Binding FilteredUsers}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          RowHeight="48"
                          GridLinesVisibility="None"
                          ScrollViewer.VerticalScrollBarVisibility="Disabled"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          Style="{StaticResource MaterialDesignDataGrid}">

                    <DataGrid.Columns>

                        <!-- USER -->
                        <DataGridTemplateColumn Header="User" Width="3*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                                        <!-- AVATAR -->
                                        <Border Width="30"
                                                Height="30"
                                                Margin="0,0,12,0"
                                                CornerRadius="15"
                                                Background="#E0E7FF">
                                            <TextBlock Text="{Binding Username, Converter={StaticResource FirstLetterConverter}}"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       TextAlignment="Center"
                                                       Foreground="#1E3A8A"/>
                                        </Border>

                                        <!-- NAME + EMAIL -->
                                        <StackPanel>
                                            <TextBlock Text="{Binding Username}" FontWeight="SemiBold" FontSize="10"/>
                                            <TextBlock Text="{Binding Email}" FontSize="8" Foreground="#6B7280"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- ROLE -->
                        <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="140" FontSize="10"/>

                        <!-- STATUS -->
                        <DataGridTemplateColumn Header="Status" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Ellipse Width="8"
                                                 Height="8"
                                                 Margin="0,0,6,0"
                                                 Fill="{Binding IsActive, Converter={StaticResource BoolToStatusBackgroundConverter}}"/>
                                        <TextBlock Text="{Binding IsActive, Converter={StaticResource BoolToStatusTextConverter}}"
                                                   FontSize="10"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- ACTION -->
                        <DataGridTemplateColumn Header="" Width="72">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                            Width="32"
                                            Height="32"
                                            Command="{Binding DataContext.EditUserCommand,
                                                RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}">
                                        <materialDesign:PackIcon Kind="PencilOutline"/>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- ================= PAGINATION ================= -->
            <Border Grid.Row="2"
                    Padding="8"
                    Background="#F9FAFB"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1,0,0,0">

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                            Command="{Binding PrevPageCommand}"
                            IsEnabled="{Binding CanGoPrevious}">
                        ◀ Prev
                    </Button>

                    <TextBlock Text="{Binding PageInfo}"
                               Margin="12,0"
                               VerticalAlignment="Center"/>

                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                            Command="{Binding NextPageCommand}"
                            IsEnabled="{Binding CanGoNext}">
                        Next ▶
                    </Button>
                </StackPanel>
            </Border>

            <!-- ================= SNACKBAR ================= -->
            <materialDesign:Snackbar Grid.RowSpan="3"
                                     MessageQueue="{Binding SnackbarMessageQueue}"
                                     VerticalAlignment="Bottom"/>
        </Grid>

        <!-- ================= ADD / EDIT DIALOG ================= -->
        <materialDesign:DialogHost.DialogContent>
            <materialDesign:Card Padding="24" Width="420">

                <StackPanel>

                    <!-- TITLE -->
                    <TextBlock Text="{Binding DialogTitle}"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="0,0,0,16"/>
                    <!--Full Name-->
                    <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"  Margin="0,12,0,0"
 materialDesign:HintAssist.Hint="Full Name"
 Text="{Binding EditUser.FullName}"/>

                    <!-- USERNAME -->
                    <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"  Margin="0,12,0,0"
                     materialDesign:HintAssist.Hint="Username"
                     Text="{Binding EditUser.Username}"/>

                    <!-- EMAIL -->
                    <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,12,0,0"
                     materialDesign:HintAssist.Hint="Email"
                     Text="{Binding EditUser.Email}"/>
                    <!-- MOBILE -->
                    <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
 Margin="0,12,0,0"
 materialDesign:HintAssist.Hint="Mobile"
 Text="{Binding EditUser.Mobile}"/>

                    <!-- PASSWORD (ADD MODE ONLY) -->
                    <Grid Margin="0,12,0,0"
                  Visibility="{Binding IsAddMode,
                               Converter={StaticResource BooleanToVisibilityConverter}}">

                        <!-- PASSWORD (HIDDEN) -->
                        <PasswordBox
                    Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                    materialDesign:HintAssist.Hint="Password"
                    materialDesign:PasswordBoxAssist.Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Padding="12,8,36,8"
                    Visibility="{Binding ShowPassword,
                                 Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>

                        <!-- PASSWORD (VISIBLE) -->
                        <TextBox
                    Style="{StaticResource MaterialDesignOutlinedTextBox}"
                    materialDesign:HintAssist.Hint="Password"
                    Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Padding="12,8,36,8"
                    Visibility="{Binding ShowPassword,
                                 Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <!-- EYE TOGGLE -->
                        <ToggleButton Width="28"
                              Height="28"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Center"
                              Margin="0,0,6,0"
                              IsChecked="{Binding ShowPassword}"
                              Style="{StaticResource MaterialDesignIconButton}">
                            <materialDesign:PackIcon Kind="Eye"/>
                        </ToggleButton>
                    </Grid>

                    <!-- PASSWORD STRENGTH BAR -->
                    <ProgressBar Height="6"
                         Margin="0,6,0,0"
                         Maximum="100"
                         Value="{Binding PasswordStrengthValue}"
                         Foreground="{Binding PasswordStrengthBrush}"
                         Style="{StaticResource MaterialDesignLinearProgressBar}"
                         Visibility="{Binding IsAddMode,
                                      Converter={StaticResource BooleanToVisibilityConverter}}"/>

                    <!-- CONFIRM PASSWORD -->
                    <PasswordBox
                Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                Margin="0,8,0,0"
                materialDesign:HintAssist.Hint="Confirm Password"
                materialDesign:PasswordBoxAssist.Password="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                Visibility="{Binding IsAddMode,
                             Converter={StaticResource BooleanToVisibilityConverter}}"/>

                    <!-- PASSWORD MISMATCH ERROR -->
                    <TextBlock Text="Passwords do not match"
                       FontSize="11"
                       Foreground="#DC2626"
                       Margin="4,4,0,0">
                        <TextBlock.Visibility>
                            <MultiBinding Converter="{StaticResource PasswordMismatchVisibilityConverter}">
                                <Binding Path="Password"/>
                                <Binding Path="ConfirmPassword"/>
                            </MultiBinding>
                        </TextBlock.Visibility>
                    </TextBlock>

                    <!-- ROLE -->
                    <ComboBox Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      Margin="0,12,0,0"
                      ItemsSource="{Binding Roles}"
                      SelectedItem="{Binding EditUser.Role}"
                      materialDesign:HintAssist.Hint="Role"/>

                    <!-- ACTIVE -->
                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <TextBlock Text="Active" Margin="0,0,12,0"/>
                        <ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}"
                              IsChecked="{Binding EditUser.IsActive}"/>
                    </StackPanel>

                    <!-- ACTIONS -->
                    <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,24,0,0">

                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Command="{Binding CancelDialogCommand}">
                            Cancel
                        </Button>

                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="12,0,0,0"
                        IsEnabled="{Binding CanSave}"
                        Command="{Binding SaveUserCommand}">
                            Save
                        </Button>
                    </StackPanel>

                </StackPanel>
            </materialDesign:Card>
        </materialDesign:DialogHost.DialogContent>


    </materialDesign:DialogHost>
</UserControl>
